// MoveValidator.js
import { Modules } from './config.js';

export class MoveValidator {
  static isValidMove(board, from, to) {
    //console.log([${Modules.MoveValidator}] Validando movimento de ${from} para ${to});
    // Aqui poderia colocar regras completas de xadrez
    return true; // placeholder
  }
}
